#pragma once

#include "../core/BookList.h"

namespace Pages {
void addBookFlow(BookList& list);
void editBookFlow(BookList& list);
void deleteBookFlow(BookList& list);
void searchBookFlow(BookList& list);
void sortFlow(BookList& list);
}  // namespace Pages
